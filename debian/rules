#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
export DH_VERBOSE=1

%:
    dh $@ --python /usr/bin/python3 --with python-virtualenv  --preinstall "pip>=8"
#,python3-gi>=3.20.0-0ubuntu1,python3-gi-cairo,gir1.2-gtk-3.0"
